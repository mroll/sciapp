create  table `experiment` (
        `id` integer primary key autoincrement,
        `qid` integer,
        `procedure` text,
        `resources` text,
        `analysis` text,
        `vars` text
);
